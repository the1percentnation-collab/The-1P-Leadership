{
  "name": "the-1p-leadership-tests",
  "private": true,
  "type": "module",
  "description": "Firestore security-rules regression tests (emulator), plus Telnyx and inbound-email unit tests.",
  "scripts": {
    "emulator": "firebase emulators:exec --only firestore --project demo-1p \"node firestore-rules.test.mjs\"",
    "test:telnyx": "node telnyx-signature.test.cjs && node telnyx-send.test.cjs && node telnyx-texml.test.cjs && node call-gate.test.cjs && node sms-consent.test.cjs",
    "test:email": "node inbound-email.test.cjs",
    "test:indexes": "node firestore-indexes.test.cjs",
    "test:crm": "node crm-freshness.test.cjs && node crm-assistant.test.cjs && node crm-ai-log.test.cjs",
    "test": "npm run test:indexes && npm run test:telnyx && npm run test:email && npm run test:crm && npm run emulator"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
