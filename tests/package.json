{
  "name": "the-1p-leadership-tests",
  "private": true,
  "type": "module",
  "description": "Firestore security-rules regression tests, run against the emulator.",
  "scripts": {
    "emulator": "firebase emulators:exec --only firestore --project demo-1p \"node firestore-rules.test.mjs\"",
    "test": "npm run emulator"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
