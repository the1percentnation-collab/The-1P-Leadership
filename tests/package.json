{
  "name": "the-1p-leadership-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore & Storage security-rules unit tests (Firebase emulator).",
  "type": "commonjs",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project the-1p-leadership-test \"mocha firestore.rules.test.js --timeout 20000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0",
    "firebase-tools": "^13.15.0",
    "mocha": "^10.7.0"
  }
}
