{
  "name": "rewrite-method-rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore security-rules tests for The Rewrite Method drip release.",
  "scripts": {
    "test": "cd ../.. && firebase emulators:exec --only firestore --project rewrite-rules-test \"node tests/rules/drip-access.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.35.1"
  }
}
